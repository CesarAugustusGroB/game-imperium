/**
 * @deprecated — the real renderer lives in `./render/BattleRenderer`.
 * This file exists as a re-export shim so older import sites
 * (`import { BattleRenderer } from './battle-renderer'`) keep working.
 */
export { BattleRenderer } from './render';
